<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.anshulgera.publicgrievanceportal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Citizen_home" />
        <activity android:name=".Admin_home" />
        <activity android:name=".LodgeGrievance" />
        <activity android:name=".LodgeReminder" />
        <activity android:name=".ActionStatus" />
        <activity android:name=".Idle"></activity>
    </application>

</manifest>